@model IEnumerable<BucksUniSite.Models.Incident>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IncidentTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IncidentLocation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IncidentDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IncidentDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Centre.ShopName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IncidentTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IncidentLocation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IncidentDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IncidentDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Centre.ShopName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IncidentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IncidentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IncidentID })
        </td>
    </tr>
}

</table>
